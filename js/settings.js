const Settings = {
    useCarImage: false,
    numCars: 10, // Only low N if using img!
    mutationFactor: 0.1,
    controlType: 'AI', // one of AI, KEYS, DUMMY
    worldGenerateTreesTryCount: 100, // 10 for quick
};
