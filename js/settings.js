const Settings = {
    useCarImage: false,
    numCars: 1, // Only low N if using img!
    controlType: 'AI', // one of AI, KEYS, DUMMY
    worldFromFile: true, // From File? Otherwise, local storage.
};

let world = null;